import type { RouteSectionProps } from "@solidjs/router";
import type { Component } from "solid-js";

export const NotFound: Component<RouteSectionProps> = () => {
  return (
    <main>
      Not found
    </main>
  );
}
